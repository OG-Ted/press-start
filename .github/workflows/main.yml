on: [push]

jobs: 
  run_script:
    runs-on: ubuntu-latest
    name: Generate files
    steps:
      - uses: lmnts-dev/press-start/.github/actions/checkout
        name: Check out repository
        