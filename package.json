{
  "devDependencies": {
    "@tinacms/cli": "^1.5.39",
    "@types/node": "^20.11.0",
    "tinacms": "^1.5.28"
  },
  "scripts": {
    "dev": "npm tinacms dev -c \"bundle exec jekyll serve\"",
    "build": " npm tinacms build && jekyll build"
  }
}
