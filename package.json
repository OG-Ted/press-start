{
  "devDependencies": {
    "@tinacms/cli": "^1.5.39",
    "@types/node": "^20.11.0",
    "tinacms": "^1.5.28"
  },
  "scripts": {
    "dev": "npx tinacms dev -c \"bundle exec jekyll serve\"",
    "build": " npx tinacms build && jekyll build"
  }
}
